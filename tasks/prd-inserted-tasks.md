- [ ] パラメトリック公理の仕組みを廃止する。公理はあくまでも論理式のプリセットを生成するだけであり、呼び出し時にモーダルでパラメータを入力させる必要はない。公理から導かれる具体的な式は、パッド上で代入操作ノードを介して表現すればよい。
    - [ ] 公理呼び出し時のモーダルUI（パラメータ入力）を削除
    - [ ] パラメトリック公理関連のロジック・型定義を整理・削除
    - [ ] 公理ノードは常にスキーマそのままの形で配置される（例: `∀x.(φ→ψ)→(φ→∀x.ψ)` のまま）
    - [ ] 具体化が必要な場合はユーザーが代入操作ノードを繋いで行う
    - [ ] 関連するストーリー・テストの更新
