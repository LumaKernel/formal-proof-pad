- [x] http://localhost:13000/ ノートブック、クエストタブ、それぞれにパスを割り当ててほしい。
  - [ ] あとさ、結局自作クエストはどこ??? 自作クエストタブができる想定なんだけど。
    - [ ] 関連したコミットとかタスク、実装の意図の確認から。
    - [x] 自作としての複製をするとエラーもでるし
          Updating a style property during rerender (border) when a conflicting property is set (borderTop) can lead to styling bugs. To avoid this, don't mix shorthand and non-shorthand properties for the same value; instead, replace the shorthand with separate values.
          src/lib/quest/CustomQuestListComponent.tsx (1278:9) @ CustomQuestList

              1276 |       )}
              1277 |       {items.length === 0 && !isCreating && !isImporting ? (
            > 1278 |         <div style={emptyStyle} data-testid="custom-quest-list-empty">
                   |         ^
              1279 |           自作クエストはまだありません。
              1280 |         </div>
              1281 |       ) : (

            Call Stack 136
            Show 130 ignore-listed frame(s)
            div
            unknown (0:0)

- [ ] ウィンドウのドラッグは、スナップは手を離したあとにゆるやかにシュってスナップされるだけでいい。動かしてる途中にもスナップされると体験が悪い
  - [ ] ここにスナップしそうだよ、というのがジワーっと弱く光るのはよさそう
- [ ] 実際のアプリのほうのキャンバスにundo/redoが実現されていないな。
  - [ ] テストが十分に様々にあるべきなので、透過的にストーリーとして十分テストできるようセットアップ
  - [ ] まずは網羅的なテストを計画する。
  - [ ] テストにさらにコーナーケースやややこしいケース、操作を含むものも追加する
  - [ ] アプリのほうでも使えるように
- [x] 解説が開ける系の実装を以前したはずだが、アプリ側に適用されていない
  - [x] 当時の実装、コミット、タスクの意図も確認しながら
  - [x] Logic Systemの名前をクリックしたら解説が開けるように
  - [x] 公理ごとの解説も作ったはずだ。
- [ ] ノート一覧でのノートのところに三点リーダーで、エクスポートみたいなメニューを作ろう。
  - [ ] ノートの新規作成の近くにも同様にリーダーメニューでインポートを用意しよう。
  - [ ] エクスポートされたデータにはバージョン情報を入れて、将来的な破壊変更に対応できる抽象化をしておこう
- [ ] 0 + 0 = 0 (模範解答) において、 (all x. x + 0 = x) -> 0 + 0 = 0 が公理として認識されている
  - (all x. phi) -> phi が公理なので、これのみが公理として認識されるべきだ。そこからの置換による解答であるべき。
  - というかこれは、 (all x. phi) -> phi[τ/x] という公理図式なのでは？これについて、 τ=0で置換している
    - 変数を表わすメタ変数と、項を表すメタ変数は衝突しないように調整してね
- [ ] 論理式編集のUIの横に論理式編集に関するドキュメントへのアクセスを置く、という昔のタスクがあるはずだが、どこにもまったくない。
  - [ ] 当時のタスク、コミット、実装の意図は？
  - [ ] 論理式編集、置換などのモーダル、などなどすべての編集のポイントで共通で出るように、編集UIコンポーネント側に追加修正
