## Codebase Patterns
- ESLintはFlat Config (`eslint.config.mjs`) を使用、eslint v9+
- `@luma-dev/luma-ts` プラグインで厳格なTSルール: `no-as-unknown-as`, `no-date`, `no-explicit-return-is`, `prefer-immutable`, `require-satisfies-in-tls`
- テンプレートリテラル内の式には `satisfies` が必要（例: `${value satisfies string}`）
- 明示的な `is` 型述語は禁止 → `find()` パターンで型ナローイングする
- プロパティには `readonly` を付ける
- Prettierフォーマット: `npm run lint-fix` で適用
- テスト: Vitest + jsdom (unit), Storybook + Playwright (browser)
- カバレッジ: v8プロバイダ、`npx vitest run --coverage --project unit`
- infinite-canvasライブラリ: 純粋ロジック(grid.ts, pan.ts) + Reactフック(usePan.ts) + コンポーネント(InfiniteCanvas.tsx)の分離パターン
- レンダーに必要な状態はuseState、イベントハンドラ内のみの状態はuseRefで管理（react-hooks/refsルール対応）
- SVGパターンでドットグリッド描画（個別dot描画よりパフォーマンス良好）
- jsdomテストでstyle.backgroundColorは`rgb(r, g, b)`形式で返る（hex不可）

# Ralph Progress Log
Started: Sun Feb 15 21:34:16 JST 2026
---

## 2026-02-15 - US-000
- 実装した内容:
  - `.trash/`ディレクトリをESLint対象外に追加
  - `require-satisfies-in-tls` エラー修正: テンプレートリテラル式に `satisfies` 追加 (layout.tsx, request.ts, Button.tsx)
  - `no-explicit-return-is` エラー修正: `isValidLocale` を `findLocale` パターンに変更 (request.ts)
  - `prefer-immutable` エラー修正: User型に `readonly` 追加 (Header.tsx, Page.tsx)
  - `react/no-unescaped-entities` エラー修正: HTMLエンティティに置換 (Page.tsx)
  - Prettierフォーマットを全体に適用
- 変更したファイル: eslint.config.mjs, src/app/layout.tsx, src/i18n/request.ts, src/stories/Button.tsx, src/stories/Header.tsx, src/stories/Page.tsx + prettierによる24ファイル
- ベースラインカバレッジ: Statements 6.66% → 完了後: Statements 6.25% (微減はrequest.tsリファクタリングによる行数変化、実質変更なし)
- **今後のイテレーションへの学び:**
  - `@luma-dev/luma-ts/require-satisfies-in-tls` はテンプレートリテラル `${}` 内のすべての式に `satisfies <type>` が必要
  - `no-explicit-return-is` は明示的な型述語 `locale is Locale` を禁止 → `Array.find()` で型を返すパターンに変更する
  - prettierが多くのファイルを変更するので、初回は `npm run lint-fix` でまとめてフォーマットしておくとよい
  - Storybookブラウザテストは `npx playwright install` が必要（未実行の場合エラーになる）
---

## 2026-02-15 - US-001
- 実装した内容:
  - `src/lib/infinite-canvas/` にライブラリの基盤を構築
  - 純粋な型定義 (`types.ts`): Point, ViewportState, Size, GridDot
  - 純粋なグリッド計算ロジック (`grid.ts`): computeGridDots, computeGridPatternParams
  - `<InfiniteCanvas>` Reactコンポーネント (`InfiniteCanvas.tsx`): SVGパターンベースのドットグリッド背景
  - ユニットテスト19件 (`grid.test.ts` + `InfiniteCanvas.test.tsx`): 全テスト100%カバレッジ
  - Storybookストーリー7種 (`InfiniteCanvas.stories.ts`): Default, ZoomedIn/Out, Panned, CustomColors, Dense/SparseGrid
  - バレルエクスポート (`index.ts`)
- 変更したファイル: src/lib/infinite-canvas/ 配下の7ファイル (新規作成)
- ベースラインカバレッジ: Statements 6.25% → 完了後: Statements 35.71% (大幅向上、新コード100%カバレッジ)
- **今後のイテレーションへの学び:**
  - SVGの`<pattern>`要素でドットグリッドを描画するのが個別dot描画より効率的
  - jsdomでは `style.backgroundColor` がhex値でなく `rgb()` 形式で返る → テストではrgb形式で比較する
  - `useId()` でSVGパターンIDを生成すると複数インスタンスの衝突を防げる
  - `satisfies string` をテンプレートリテラル内で忘れやすいので、SVGのurl()参照等に注意
---

## 2026-02-15 - US-002
- 実装した内容:
  - パン操作の純粋ロジック (`pan.ts`): `applyPanDelta`, `computeDelta`
  - Reactフック (`usePan.ts`): PointerEvents統合、ドラッグ状態管理、ポインターキャプチャ
  - `<InfiniteCanvas>` にパン統合: `onViewportChange` props、`grab`/`grabbing`カーソル、`touchAction: none`
  - ユニットテスト追加 (`pan.test.ts` 7件、`InfiniteCanvas.test.tsx` に9件追加): パン動作、右クリック無視、ポインターキャプチャ
  - インタラクティブStorybook (`PannableCanvas.stories.tsx`): useState連携のパンデモ
  - `usePan.ts` のリファクタリング: `isDragging` を `useRef` から `useState` に変更（`react-hooks/refs` ルール対応）
  - `types.ts` をカバレッジ除外に追加（型定義のみファイル）
  - バレルエクスポート更新 (`index.ts`)
- 変更したファイル: src/lib/infinite-canvas/ (pan.ts, pan.test.ts, usePan.ts, PannableCanvas.stories.tsx 新規、InfiniteCanvas.tsx, InfiniteCanvas.test.tsx, index.ts 変更), vitest.config.ts
- ベースラインカバレッジ (infinite-canvas): Stmts 95.16%, Branch 80%, Funcs 78.94% → 完了後: Stmts 100%, Branch 90.47%, Funcs 100%
- **今後のイテレーションへの学び:**
  - `react-hooks/refs` ルール: レンダー中に `ref.current` を読むとエラー → レンダーに必要な値は `useState` で管理する
  - jsdomでは `setPointerCapture`/`releasePointerCapture` が未実装 → `Element.prototype` にモックを追加してテスト
  - PointerEvents を使うと mouse + touch を統一的に扱える。`touchAction: none` で既定のタッチ動作を抑制
  - パンロジックは純粋関数 (pan.ts) として分離し、フックから呼ぶパターンがテスタビリティに優れる
---
