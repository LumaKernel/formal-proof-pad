# prd-docs-improve (archived)

- [x] ビルトインのドキュメントは、強調の `**`, `*`, `__` などはすべて `<b>`, `<i>` などで、 インラインコードも `<code>` で書くようにすべて完全に統一してください。
  - [x] 強制するように簡単なチェッカー(スクリプト、もしくはテストによるインポートしてマッチしての一括チェック)も作ってCIで確認されるようにしてください。
