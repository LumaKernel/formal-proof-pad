# 現在のタスク

**出典:** `tasks/prd-inserted-tasks.md` 1行目

> ノートAから証明図をコピーして、別の(互換性のないタイプの)ノートBへペーストしようとするが、互換性がない旨のエラーが表示される、というストーリーを作成しよう。必要に応じて機能追加もしよう。

## テスト計画

- 純粋ロジックのテスト（`copyPasteLogic.test.ts`）は既に十分なカバレッジがある → 追加不要
- ストーリーの play 関数でインタラクションテストを行う

## ストーリー計画

### 1. 機能改善: `alert()` → インラインエラーメッセージ

現在、互換性エラーは `alert()` で表示されている。これだと:

- Storybook play関数でブロックされる
- UXが悪い（モーダルダイアログで操作が中断される）

→ `ProofWorkspace` 内にトースト風のエラーメッセージを `state` で管理し、数秒後に自動消去する仕組みに変更する。`data-testid` 付きでテスト可能にする。

### 2. ストーリー: `CopyPasteIncompatible`

`ProofWorkspace.stories.tsx` に追加:

- Hilbert系（lukasiewicz）と自然演繹（NJ）の2つのワークスペースを左右に並べたラッパーコンポーネント
- play関数:
  1. 左(Hilbert)でMP適用済みノードをコピー
  2. 右(自然演繹)でペースト操作
  3. エラーメッセージが表示されることを確認
  4. 新しいノードがペーストされない（ノード数変化なし）ことを確認
