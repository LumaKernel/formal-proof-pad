## 申し送り

**出典**: `tasks/prd-inserted-tasks.md` L20-22, L42

純粋ロジック層（substitutionApplicationLogic.ts, workspaceState.ts統合）は完了。
次のイテレーションで以下が必要:

1. ProofWorkspace.tsx のコンテキストメニューに「Apply 代入操作」を追加（UI統合）
2. 代入エントリのUI入力フォーム（SubstitutionEntryEditor的なコンポーネント）
3. EditableProofNode での代入ノード表示対応
4. Storybook ストーリー追加
