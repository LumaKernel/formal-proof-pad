## 申し送り事項

**出典**: `tasks/prd-inserted-tasks.md` line 26

### 残タスク

- [ ] A4/A5スキーマノード: パラメトリック公理呼び出しの特別UI設計
  - A4 `∀x.φ → φ[t/x]` のスキーマはメタレベル記述でオブジェクト言語で書けない
  - 現在は「φを代入済みのA4インスタンス（τを含む）」を手動でノードに記述し、代入操作で具体化するワークアラウンド
  - 理想的にはパレットからA4を呼び出す際にφ/tのパラメータを指定するUIが必要
  - A5 `∀x.(φ→ψ) → (φ→∀x.ψ)` も同様
