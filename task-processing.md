## タスク（from tasks/inserted-tasks.md）

- [ ] エラーになってる状態の論理式ノードがノーマル状態で分かりにくい
- [ ] エラーになってる状態の論理式ノードがあるなら、フルストーリーは失敗すべきだ

### 背景

カットの基本: 推移律 のクエストの模範解答で、⇒ が Unexpected Character で怒られている。
readonly モードでパースエラーのある論理式ノードが、正常なノードと視覚的に区別がつかない。

### テスト計画

- `EditableProofNode.test.tsx` にパースエラー時の readonly 表示テストを追加
  - エラー表示のスタイル（赤い枠線/波線）が適用されることを確認
  - 空テキストはエラー表示しないことを確認
- フルストーリーで parse error があるノードが存在する場合に失敗するアサーションを追加

### ストーリー計画

- `EditableProofNode.stories.tsx` にパースエラー readonly ノードのストーリーを追加
