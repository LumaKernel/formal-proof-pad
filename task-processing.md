## 実行中タスク

**元ファイル:** `tasks/prd-inserted-tasks.md`

> Gen適用の変数を指定するところは、事前に選ぶのではなく、対象の論理式を選んだあとに、選択式にできないだろうか。
> このとき、あくまでも選択式は補完ということにして、自由入力もできるようにする

### 現状分析

- **ヘッダーパス**: テキスト入力で変数名入力 → "Apply Gen"ボタン → ノードクリック → 即適用
- **コンテキストメニューパス**: ノード右クリック → "Apply Gen" → プロンプトバナーで変数名入力
- コンテキストメニューパスは既に「対象の論理式を選んだあと」のフロー

### 実装計画

1. **ヘッダーのGen UIを変更**: 変数テキスト入力を削除し、Genボタンクリックで直接「ノード選択モード」に入る
2. **ノードクリック後のフロー統一**: ヘッダーパスもコンテキストメニューパスも同じプロンプトモーダルを使う
3. **プロンプトモーダルに自由変数サジェスト追加**: `freeVariablesInFormula` で前提式の自由変数を抽出し、チップとして表示
4. **純粋ロジック**: `genApplicationLogic.ts` に `extractPremiseFreeVariables` ヘルパーを追加

### テスト計画

- **genApplicationLogic.test.ts**: `extractPremiseFreeVariables` のテスト追加
- **ProofWorkspace.test.tsx**: Gen prompt での自由変数サジェスト表示のテスト（既存テストの更新）
- **ProofWorkspace.stories.tsx**: Gen適用ストーリーの更新（UI変更を反映）

### ストーリー計画

- 既存の Gen 関連ストーリーの更新（UI変更があるため）
